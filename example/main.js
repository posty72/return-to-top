import { ReturnToTop } from '../dist/index';

const _returnToTop = new ReturnToTop({
    arrowColour: '#FFFFFF',
    placement: 'right',
    color: '#000000',
    shape: 'circle',
    animation: {
        fade: true,
        rotate: true
    }
})
